import { createDefaultTextarea } from 'shared/form-controls/textarea/defaultTextarea';

export const textareaState = {
	body: createDefaultTextarea('Текст отзыва', 'Ваш отзыв не может быть пустым'),
};
